<%= form_with(model: log) do |form| %>
  <% if log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(log.errors.count, "error") %> prohibited this log from being saved:</h2>

      <ul>
        <% log.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :timestamp %>
    <%= form.datetime_select :timestamp %>
  </div>

  <div class="field">
    <%= form.label :annual_salary %>
    <%= form.text_field :annual_salary %>
  </div>

  <div class="field">
    <%= form.label :monthly_income_tax %>
    <%= form.text_field :monthly_income_tax %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
